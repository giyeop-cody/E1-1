## Docker 컨테이너 내 터미널 접속
컨테이너의 터미널에 접속하고 싶어서 docker run -it hello-world를 했지만 해당 컨테이너는 1회성 동작을 하고 백그라운드에서 동작하지 않아서 터미널로 접속할수 없었음.

대신 ubuntu 이미지를 사용한 컨테이너로 변경하여 터미널 접속에 성공


## 바운트 마운트 실행 명령+호스트 변경 전/후 비교
컨테이너가 실행 중인 호스트를 변경하라는지 호스트에서 파일을 변경하라는지 정확히 파악되지 않았음.

평가 진행을 통해서 2번째로 판단하여 그렇게 진행함.